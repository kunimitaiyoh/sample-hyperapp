import { Link } from "@hyperapp/router";
import { ActionResult, ActionsType, app, h, View } from "hyperapp";

// export interface IArticleActions {
// }

// export const articleActions: IArticleActions = {
// };

export const ArticleView = (actions: {}) => (
  <div>
    <h2>Article</h2>
  </div>
);
